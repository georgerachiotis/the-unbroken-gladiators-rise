# Application icon

`app-icon.png` is the high-resolution source used by the JavaFX window.
`app-icon.ico` contains Windows icon sizes from 16x16 through 256x256 and is
used by `jpackage` for the executable.
